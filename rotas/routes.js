// import express from 'express';  // Importa o módulo 'express', um framework web para Node.js
// import authentication from '../controllers/authentication'; // Importa o 'apicontroller'
// import authMiddleware from '../middlewares/middleware'; // Importa o 'authMiddleware'

// const router = express.Router();  // Cria um novo roteador

// // Definindo uma rota para obter todas as transações
// router.get('/', autenticarToken, clientesController.getAllClientes);

// // Definindo uma rota para adicionar uma nova transação
// router.post('/', authMiddleware, transactionsController.addTransaction);

// // Definindo uma rota para atualizar uma transação existente (substituição completa)
// router.put('/:id', authMiddleware, transactionsController.updateTransactionPut);

// // Definindo uma rota para atualizar uma transação existente (substituição parcial)
// router.patch('/:id', authMiddleware, transactionsController.updateTransactionPatch);

// // Definindo uma rota para deletar uma transação
// router.delete('/:id', authMiddleware, transactionsController.deleteTransaction);

// // Exportando o objeto 'router' para uso em outros arquivos
// export default router;
